# Vite dev-server proxy target for the AI-GYM backend.
# The app itself talks same-origin relative (/api, /ws) — this only steers
# the dev proxy. Point at a remote backend when developing against one.
VITE_API_TARGET=http://localhost:8000
